    import { createContext } from "react";

    //we use noteContext in order to wraps the components that are to be used by others
    const noteContext = createContext();

    export default noteContext;